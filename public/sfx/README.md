# SFX — kısa ses efektleri

Bu klasöre aşağıdaki dosyayı ekleyin:

- **page-turn.mp3** — sayfa çevirme sesi (planner flip). Kısa bir kağıt çevirme sesi (örn. mevcut `public/audio/paper-flip.mp3` kopyalanıp `page-turn.mp3` olarak kaydedilebilir).

Uygulama sayfa çevirme sesi için `/sfx/page-turn.mp3` yolunu kullanır.
